{
  "name": "supermemory-firing-range",
  "version": "1.0.0",
  "scripts": {
    "check-types": "echo Type checking... && exit 0",
    "format-lint": "echo Format/lint... && exit 0",
    "preinstall": "echo [SAFE] preinstall hook ran - in real attack this executes attacker code"
  },
  "devDependencies": {}
}
